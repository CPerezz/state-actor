diff --git a/execution/commitment/commitmentdb/commitment_context.go b/execution/commitment/commitmentdb/commitment_context.go
index 36836bd009..0eccdc1fa9 100644
--- a/execution/commitment/commitmentdb/commitment_context.go
+++ b/execution/commitment/commitmentdb/commitment_context.go
@@ -303,6 +303,12 @@ func (sdc *SharedDomainsCommitmentContext) ComputeCommitment(ctx context.Context
 	if sdc.pendingUpdate != nil {
 		panic("sdCtx.ComputeCommitment called directly with non-nil pendingUpdate; use SharedDomains.ComputeCommitment wrapper instead")
 	}
+	if blockNum >= 1 && blockNum <= 3 {
+		log.Warn("[INST-B2] ComputeCommitment ENTRY",
+			"block", blockNum, "txNum", txNum, "saveState", saveState,
+			"mode", sdc.updates.Mode(), "deferCommitmentUpdates", sdc.deferCommitmentUpdates,
+			"sd", fmt.Sprintf("%p", sdc.sharedDomains))
+	}
 	if dbg.KVReadLevelledMetrics {
 		mxCommitmentRunning.Inc()
 		defer mxCommitmentRunning.Dec()
@@ -318,6 +324,11 @@ func (sdc *SharedDomainsCommitmentContext) ComputeCommitment(ctx context.Context
 			keysPerSec = uint64(float64(updateCount) / took.Seconds())
 		}
 		log.Debug("[commitment] processed", "block", blockNum, "txNum", txNum, "keys", common.PrettyCounter(updateCount), "keys/s", common.PrettyCounter(keysPerSec), "mode", sdc.updates.Mode(), "spent", took, "rootHash", hex.EncodeToString(rootHash))
+		if blockNum >= 1 && blockNum <= 3 {
+			log.Warn("[INST-B2] ComputeCommitment EXIT",
+				"block", blockNum, "txNum", txNum, "mode", sdc.updates.Mode(),
+				"rootHash", hex.EncodeToString(rootHash), "keys", updateCount, "err", err)
+		}
 	}()
 	if updateCount == 0 {
 		rootHash, err = sdc.patriciaTrie.RootHash()
@@ -801,6 +812,14 @@ func (sdc *TrieContext) Branch(pref []byte) ([]byte, kv.Step, error) {
 	if err != nil {
 		return nil, 0, err
 	}
+	if sdc.txNum <= 12 {
+		prefHex := hex.EncodeToString(pref)
+		if len(prefHex) > 32 {
+			prefHex = prefHex[:32] + "..."
+		}
+		log.Warn("[INST-B2] Branch READ",
+			"txNum", sdc.txNum, "prefix", prefHex, "retLen", len(enc), "step", step)
+	}
 	// Branch reads feed Merge(prev,update), branchEncoder/merger internal buffers,
 	// deferred-update queues, and unfoldBranchNode reads. The slice returned by the
 	// underlying state cache / getter aliases shared storage that another goroutine
@@ -811,8 +830,25 @@ func (sdc *TrieContext) Branch(pref []byte) ([]byte, kv.Step, error) {
 
 func (sdc *TrieContext) PutBranch(prefix []byte, data []byte, prevData []byte) error {
 	if sdc.stateReader.WithHistory() { // do not store branches if explicitly operate on history
+		if sdc.txNum <= 12 {
+			prefHex := hex.EncodeToString(prefix)
+			if len(prefHex) > 32 {
+				prefHex = prefHex[:32] + "..."
+			}
+			log.Warn("[INST-B2] PutBranch SKIPPED (WithHistory=true)",
+				"txNum", sdc.txNum, "prefix", prefHex, "dataLen", len(data))
+		}
 		return nil
 	}
+	if sdc.txNum <= 12 {
+		prefHex := hex.EncodeToString(prefix)
+		if len(prefHex) > 32 {
+			prefHex = prefHex[:32] + "..."
+		}
+		log.Warn("[INST-B2] PutBranch WRITE",
+			"txNum", sdc.txNum, "prefix", prefHex, "dataLen", len(data),
+			"prevLen", len(prevData), "viaCollector", sdc.localCollector != nil)
+	}
 	if sdc.trace {
 		fmt.Printf("[SDC] PutBranch: %x: %x\n", prefix, data)
 	}
diff --git a/execution/stagedsync/committer.go b/execution/stagedsync/committer.go
index 6c1031bd67..ce2c7672a6 100644
--- a/execution/stagedsync/committer.go
+++ b/execution/stagedsync/committer.go
@@ -481,6 +481,10 @@ func (cc *commitmentCalculator) publish(ctx context.Context, r commitmentResult)
 // when defer mode is on) with the block's hash, so the next call's
 // FlushPendingUpdates uses the same hash-aware routing.
 func (cc *commitmentCalculator) computeWithBlockAccumulator(ctx context.Context, br *blockResult) ([]byte, error) {
+	if br.BlockNum >= 1 && br.BlockNum <= 3 {
+		log.Warn("[INST-B2] BUILDER-WRAP computeWithBlockAccumulator START (has accumulator-lock)",
+			"block", br.BlockNum, "blockHash", br.BlockHash, "lastTxNum", br.lastTxNum)
+	}
 	defer func() {
 		// Stamp the pending update (if any was set during ComputeCommitment)
 		// with this block's hash so FlushPendingUpdates on the next call
diff --git a/execution/stagedsync/exec3.go b/execution/stagedsync/exec3.go
index 179e5f5d10..1c8b3c0583 100644
--- a/execution/stagedsync/exec3.go
+++ b/execution/stagedsync/exec3.go
@@ -740,6 +740,11 @@ type FlushAndComputeCommitmentTimes struct {
 
 // computeAndCheckCommitmentV3 - does write state to db and then check commitment
 func computeAndCheckCommitmentV3(ctx context.Context, header *types.Header, applyTx kv.TemporalRwTx, doms *execctx.SharedDomains, cfg ExecuteBlockCfg, e *StageState, parallel bool, logger log.Logger, u Unwinder) (ok bool, times FlushAndComputeCommitmentTimes, err error) {
+	if header != nil && header.Number.Uint64() >= 1 && header.Number.Uint64() <= 3 {
+		log.Warn("[INST-B2] VALIDATOR-PATH computeAndCheckCommitmentV3 START (no accumulator-lock)",
+			"block", header.Number.Uint64(), "parallel", parallel,
+			"headerStateRoot", header.Root.Hex(), "sd", fmt.Sprintf("%p", doms))
+	}
 	if header == nil {
 		return false, times, errors.New("header is nil")
 	}
